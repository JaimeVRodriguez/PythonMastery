# Tuples is read only list - Not mutable
point = (1, 2, 3)
print(point[0])

point = (1, 2) + (3, 4)
print(point)

# Basically do anything with as with a list
# Just cant modify in any way
# Use when you don't want your "list" to be changed on accident
